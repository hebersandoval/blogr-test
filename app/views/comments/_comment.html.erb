<p><em><%= comment.user.name %> said:</em></p>
<p><%= comment.body %></p>
<% if @can_moderate %>
  <p>
    <%= link_to 'Delete', comment_path(comment), method: :delete, class: "btn btn-default" %>
  </p>
<% end %>
